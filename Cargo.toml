[workspace]
resolver = "2"
members = ["app", "shared"]
default-members = ["app"]
# workers/ は wasm ターゲットで wrangler / worker-build から別ビルドするため、
# native のワークスペースからは除外する。型は shared/ を path 依存で共有する。
exclude = ["workers"]
